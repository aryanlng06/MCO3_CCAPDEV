<html>
<head>
    <title>Manage Users</title>
</head>
<body>
    <h1>Manage Users</h1>
    <!-- User list with delete buttons -->
    <table>
        <thead>
            <tr>
                <th>Username</th>
                <th>Email</th>
                <th>Delete</th>
            </tr>
        </thead>
        <tbody>
            {{#each users}}
            <tr>
                <td>{{username}}</td>
                <td>{{email}}</td>
                <td><input type="button" class="del-user-admin" value="Delete user" data-user-id="{{_id}}"></a></td>
            </tr>
            {{/each}}
        </tbody>
    </table>

    <a href="/admin_page.handlebars">Back to Admin Page</a>
</body>
</html>