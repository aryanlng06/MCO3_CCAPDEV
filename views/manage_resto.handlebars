<html>
<head>
    <title>Manage Restaurants</title>
</head>
<body>
    <h1>Manage Restaurants</h1>
    <h2>Add Restaurant</h2>
    <!-- Add restaurant form -->
    <form action="/addRestAdmin" method="POST">
        <!-- Add your form fields for adding a restaurant -->
        <!-- Example: -->
        <label for="restaurantName">Restaurant Name:</label>
        <input type="text" id="restaurantName" name="restaurantName" required>
        <!-- Add more fields as needed -->
        <button type="submit">Add Restaurant</button>
    </form>

    <h2>Delete Restaurant</h2>
    <!-- Restaurant list with delete buttons -->
    <table>
        <thead>
            <tr>
                <th>Restaurant Name</th>
                <th>Delete</th>
            </tr>
        </thead>
        <tbody>
            {{#each kitchens}}
            <tr>
                <td>{{restaurantName}}</td>
                <td><input type="button" class="del-Btn-admin" value="Delete Resto" data-resto-id="{{_id}}"></a></td>
            </tr>
            {{/each}}
        </tbody>
    </table>

    <a href="/admin_page.handlebars">Back to Admin Page</a>
</body>
</html>